package paquetesModificadoresAccesoConvenciónNombres;

public class RestriccionModificadorProtected {

	/*
	 * protected puede ser accedido (a través de la herencia) por una subclase
	 * incluso si la subclase está en un paquete diferente.
	 * 
	 * Para una subclase fuera del paquete, el miembro protected solo puede ser
	 * accedido por herencia
	 * 
	 * El indicador protected indica que los elementos solo pueden ser accedidos
	 * desde el mismo paquete, igual que el modificador por defecto, sin embargo
	 * tambien puede ser accedido desde cualquier clase que herede
	 * independientemente si esta en el mismo paquete o no.
	 */
}
